# QEMU build tree (keep outside this repo or in a sibling directory)
build/
qemu-upstream/

# Compiled objects and binaries
*.o
*.a
*.so
qemu-system-riscv64
qemu-riscv64

# Generated files
config-host.mak
config-target.mak
*.d
